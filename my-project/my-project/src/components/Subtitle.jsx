// Subtitle, el cual debe renderizar un elemento h2 con un subtítulo (pasado por prop)

import React from 'react';

function Subtitle({ subtitle }) {
  return <h2>{subtitle}</h2>;
}

export default Subtitle;
